/**
 * Environment Configuration
 * -------------------------
 * Centralizes the API base URL so it can easily switch
 * between development and production environments.
 */

export const environment = {
    production: false,
    apiUrl: 'http://localhost:5001/api',
};
